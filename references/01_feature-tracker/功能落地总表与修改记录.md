# 团队管理软件功能落地总表与修改记录

> 本文档用于持续记录“团队管理软件 / 团队项目管理平台”的功能完成情况、缺口、每次修改内容、验证结果和下一步计划。  
> 每次 Codex、TRAE 或人工修改后，必须同步更新本文档。  
> 建议存放位置：`references/feature-tracker/功能落地总表与修改记录.md`

---

## 0. 文档维护规则

### 0.1 每次修改后必须更新

每次改动后，至少补充以下内容：

1. 修改日期与操作者；
2. 修改目标；
3. 修改文件；
4. 涉及模块；
5. 是否改变接口字段；
6. 是否改变权限规则；
7. 是否影响已有演示数据；
8. 执行了哪些验证命令；
9. 截图验收了哪些页面；
10. 新增问题；
11. 剩余问题；
12. 下一步建议。

### 0.2 状态标记

| 状态 | 含义 |
|---|---|
| 已完成 | 后端、前端、数据、权限、验证基本闭环 |
| 部分完成 | 有模型/接口/页面之一或部分流程可用，但未闭环 |
| 仅预留 | 有菜单、依赖、占位字段或文档设想，但不能作为已完成 |
| 未开始 | 尚未实现 |
| 需复核 | 已实现但未经过截图、API、权限或数据验证 |
| 暂缓 | 当前阶段不建议做 |

### 0.3 优先级标记

| 优先级 | 含义 |
|---|---|
| P0 | 不修无法演示或核心流程不可用 |
| P1 | 影响主要演示链路或业务闭环 |
| P2 | 影响完整度、体验或真实使用 |
| P3 | 美化、增强、低风险优化 |
| P4 | 远期规划 |

---

## 1. 当前版本状态

| 项目 | 内容 |
|---|---|
| 当前主线 | 新写的团队项目管理平台，不是旧 GitHub 仓库 TeamFilePermissionCenter |
| 当前阶段 | v0.8.2-ui-recovery 冻结前验收 |
| 当前原则 | 先稳定可用，再补关键模块，最后美化 |
| 当前重点 | 本地 Docker 验证、V2 演示数据验收、功能落地对照 |
| 禁止事项 | 不随意改接口字段、权限规则、登录 token、敏感资料流程、经费公开规则 |

---

## 2. 功能落地总表

| 模块 | 后端模型 | API | 前端页面 | 演示数据 | 权限验证 | 截图验收 | 当前状态 | 优先级 | 备注 |
|---|---|---|---|---|---|---|---|---|---|
| 登录与权限 | 是 | 是 | 是 | 是 | 需复核 | 需复核 | 已完成 | P0 | 需 Docker 环境复核 |
| Dashboard | 是 | 是 | 是 | 是 | 需复核 | 需复核 | 已完成/需复核 | P0 | 已修复 P3 UI 回归 |
| 项目管理 | 是 | 是 | 是 | 是 | 需复核 | 需复核 | 已完成 | P0 | V2 数据含 6 个项目 |
| 比赛管理 | 是 | 是 | 是 | 是 | 需复核 | 需复核 | 部分完成 | P1 | 还缺比赛矩阵、晋级漏斗 |
| 任务管理 | 是 | 是 | 是 | 是 | 需复核 | 需复核 | 已完成 | P1 | 需看逾期、分配、状态流转 |
| 成员管理 | 是 | 是 | 是 | 是 | 需复核 | 需复核 | 部分完成 | P1 | 成员个人主页、忙闲状态未完善 |
| 经费管理 | 是 | 是 | 是 | 是 | 需复核 | 需复核 | 已完成/需复核 | P0 | 重点看是否还有 ¥NaN |
| 文件资料 | 是 | 是 | 是 | 是 | 需复核 | 需复核 | 部分完成 | P1 | 在线预览、版本对比可增强 |
| 文件版本 | 是 | 是 | 是 | 是 | 需复核 | 需复核 | 部分完成 | P1 | 需确认变更记录是否进入时间线 |
| 贡献记录 | 是 | 是 | 是 | 是 | 需复核 | 需复核 | 已完成 | P1 | 需验证负责人审核 |
| 成员排序 | 是 | 是 | 是 | 是 | 需复核 | 需复核 | 已完成 | P1 | 需验证异议流程 |
| 异议处理 | 是 | 是 | 是 | 是 | 需复核 | 需复核 | 部分完成 | P1 | 需确认通知与日志 |
| 知识产权申请 | 是 | 是 | 是 | 是 | 需复核 | 需复核 | 部分完成 | P1 | 高级流程和报告导出未完善 |
| 敏感资料审批 | 是 | 是 | 是 | 是 | 需复核 | 需复核 | 已完成/需安全复核 | P0 | 默认脱敏、审批限时、日志必须验 |
| 通知中心 | 是 | 是 | 是 | 是 | 需复核 | 需复核 | 部分完成 | P1 | 定时提醒未完整闭环 |
| 操作日志 | 是 | 是 | 是 | 是 | 需复核 | 需复核 | 已完成 | P1 | 需验证敏感查看记录 |
| 导入导出 | 是 | 是 | 是 | 部分 | 需复核 | 需复核 | 部分完成 | P2 | 复杂导入、Word/PDF 报告未完成 |
| 用户管理 | 是 | 是 | 是 | 是 | 需复核 | 需复核 | 已完成 | P1 | 需验证角色筛选 |
| 移动端适配 | 否 | 否 | 是 | 是 | 需复核 | 需复核 | 部分完成 | P0 | 重点看是否显示 PC 侧栏 |
| 公共展示主页 | 部分/未知 | 部分/未知 | 部分/未知 | 部分 | 未验证 | 未验证 | 未完成/仅预留 | P2 | 最初文档重点之一 |
| 项目日历 | 部分/未知 | 部分/未知 | 仅预留 | 有 JSON 示例 | 未验证 | 未验证 | 未完成 | P1 | FullCalendar/Gantt 未真正落地 |
| 横向项目历程条 / Gantt | 否/未知 | 否/未知 | 否/未知 | 有 JSON 示例 | 未验证 | 未验证 | 未完成 | P1 | 需单独作为 P3.5 |
| 成员个人主页 | 部分 | 部分 | 部分 | 部分 | 未验证 | 未验证 | 部分完成 | P2 | 成长时间线未完善 |
| 灵活工作时间 | 部分/未知 | 部分/未知 | 部分/未知 | 未知 | 未验证 | 未验证 | 未完成/部分完成 | P2 | 需核查 |
| 比赛晋级漏斗 | 否/未知 | 否/未知 | 否/未知 | V2 有结果数据 | 未验证 | 未验证 | 未完成 | P1 | 需新增矩阵/漏斗视图 |
| 项目复盘归档 | 部分/未知 | 部分/未知 | 部分/未知 | 未知 | 未验证 | 未验证 | 未完成/部分完成 | P2 | 需核查 |
| Word/PDF/Excel 报告导出 | 部分 | 部分 | 部分 | 部分 | 未验证 | 未验证 | 部分完成 | P2 | 项目完整报告导出需增强 |
| Celery 定时提醒 | 有依赖 | 部分/未知 | 否 | 否 | 未验证 | 不适用 | 部分完成 | P1 | 需 Docker 验证 worker/beat |
| Docker/Nginx/服务器部署 | 未知 | 不适用 | 不适用 | 不适用 | 未验证 | 未验证 | 未完成/进行中 | P0 | 当前下一步重点 |

---

## 3. 关键缺口清单

### 3.1 P0：必须先完成

- [ ] 修正 `requirements/base.txt` 中错误的 `pip install ...` 行；
- [ ] 完成本地 Docker Compose 验证；
- [ ] Docker 环境下 `python manage.py check` 通过；
- [ ] Docker 环境下 `npm run build` 通过；
- [ ] Docker 环境导入 V2 seed；
- [ ] Docker 环境完成全量截图；
- [ ] 确认无 `¥NaN`、`undefined`、表头冒号、标题竖排、内容区压缩；
- [ ] 移动端 375px 不显示 PC 左侧栏；
- [ ] 冻结 `v0.8.2-ui-recovery`。

### 3.2 P1：演示版关键增强

- [ ] 项目日历；
- [ ] 横向项目历程条 / Gantt；
- [ ] 统一 timeline-events 聚合接口；
- [ ] 比赛矩阵视图；
- [ ] 比赛晋级漏斗；
- [ ] 项目详情中的阶段/文件/成员/比赛/经费/知识产权统一时间线；
- [ ] 敏感资料安全复核；
- [ ] Celery worker / beat 定时提醒验证。

### 3.3 P2：完整度增强

- [ ] 公共展示主页；
- [ ] 成员个人主页；
- [ ] 成员成长时间线；
- [ ] 灵活工作时间；
- [ ] 项目复盘归档；
- [ ] 文件在线预览增强；
- [ ] Word/PDF/Excel 报告导出增强；
- [ ] 项目成果公开展示；
- [ ] 成员风采展示。

### 3.4 P3：美化与体验

- [ ] 页面局部美化；
- [ ] 图表视觉优化；
- [ ] 空状态优化；
- [ ] 表格筛选体验优化；
- [ ] 移动端细节优化；
- [ ] 但必须遵守：不再大范围全局覆盖 Element Plus，不再引入 px-to-vw。

---

## 4. 每次修改记录模板

> 每次修改后复制以下模板追加一条记录。

### 修改记录：YYYY-MM-DD HH:mm

| 项目 | 内容 |
|---|---|
| 操作者 | Codex / TRAE / 人工 |
| 修改目标 |  |
| 涉及模块 |  |
| 修改文件 |  |
| 是否改后端模型 | 是 / 否 |
| 是否改 API 字段 | 是 / 否 |
| 是否改权限规则 | 是 / 否 |
| 是否改登录 token | 是 / 否 |
| 是否改敏感资料流程 | 是 / 否 |
| 是否影响经费公开规则 | 是 / 否 |
| 是否影响 V2 seed | 是 / 否 |
| 是否需要迁移 | 是 / 否 |
| 是否需要更新文档 | 是 / 否，必须说明更新了哪里 |

#### 修改内容

- 

#### 验证命令

```bash
# 后端
python manage.py check

# 前端
npx vue-tsc --noEmit
npm run build

# Docker
docker compose build
docker compose up -d
docker compose ps
```

#### 页面截图验收

- [ ] 登录页；
- [ ] Dashboard；
- [ ] 项目列表；
- [ ] 项目详情；
- [ ] 比赛列表；
- [ ] 任务列表；
- [ ] 经费页；
- [ ] 文件页；
- [ ] 贡献页；
- [ ] 成员排序；
- [ ] 知识产权；
- [ ] 敏感资料；
- [ ] 通知；
- [ ] 操作日志；
- [ ] 移动端 375px。

#### 新增问题

- 

#### 剩余问题

- 

#### 下一步

- 

---

## 5. 版本路线图

| 版本 | 目标 | 状态 |
|---|---|---|
| v0.8.1-stable | P0/P1/P2 功能稳定版 | 已冻结 |
| v0.8.2-ui-recovery | P3 UI 回归修复 + V2 seed + Docker 前验收 | 进行中 |
| v0.8.3-demo-ready | 演示版完善，可稳定给老师/同学看 | 未开始 |
| v0.9.0-calendar-timeline | 项目日历与横向历程条 | 未开始 |
| v0.9.1-competition-matrix | 比赛矩阵与晋级漏斗 | 未开始 |
| v0.9.2-public-portal | 公共展示主页 | 未开始 |
| v1.0.0-production-ready | 部署稳定、权限审计、长期使用 | 未开始 |
| v1.1.0-mobile-smart | 移动端智能跳转 + 任务全流程移动操作 | 未开始 |
| v1.2.0-personalization | 账户级个人化配置（跨设备同步） | 未开始 |

---

## 5.1 未来需求规划（2026-07-03 新增）

> 以下四个需求由用户提出，作为 v1.1 / v1.2 阶段的重点方向，暂不在当前 v0.8.x 演示版中实现。

### 需求 A：移动端智能跳转（v1.1.0-mobile-smart）

**目标**：移动端主页的展示卡片/图表可点击，直接跳转到对应功能页面。

**具体要求**：

1. Dashboard 移动端的统计卡片（任务数、经费、项目数等）支持点击跳转：
   - 点击"任务数"卡片 → 跳转任务列表
   - 点击"经费"卡片 → 跳转经费总览
   - 点击"项目数"卡片 → 跳转项目列表
   - 点击"风险预警"卡片 → 跳转风险详情
2. Dashboard 的比赛节点日历条目支持点击跳转到比赛详情
3. 通知列表条目支持点击跳转到关联业务页面（如经费通知→经费详情）
4. 移动端底部导航栏增加"快捷操作"入口，可快速创建任务、提交贡献

**技术方向**：

- 前端：为移动端 Dashboard 卡片增加 `@click` 路由跳转，使用 `router.push()`
- 移动端增加快捷操作浮动按钮（FAB），类似微信浮窗
- 通知中心增加 `action_url` 字段，前端根据此字段跳转

### 需求 B：任务全流程移动端操作（v1.1.0-mobile-smart）

**目标**：发送、接收、处理任务等核心操作在手机端完整可用，不只是查看。

**具体要求**：

1. 移动端可创建任务（标题、描述、指派给、截止日期、优先级）
2. 移动端可修改任务状态（待办→进行中→已完成→已逾期）
3. 移动端可查看任务详情、附件、评论
4. 移动端可接收任务通知并快速操作（接受/拒绝/完成）
5. 移动端可查看任务看板（简化版，支持拖拽或按钮切换状态）

**技术方向**：

- 前端：移动端新增 `MobileTaskCreateView.vue` / `MobileTaskDetailView.vue`
- 移动端任务看板用横向滑动卡片替代 PC 端拖拽
- 后端 API 无需修改，复用现有 `/api/v1/tasks/` 接口
- 通知推送中携带 `task_id`，点击直接打开任务详情

### 需求 C：账户级个人化配置（v1.2.0-personalization）

**目标**：每个账户可自定义主页样式和布局，配置跟随账户跨设备同步，不同账户在同一设备各有独立配置。

**具体要求**：

1. 用户可自定义 Dashboard 卡片布局（拖拽排序、显示/隐藏卡片）
2. 用户可选择主题色调（从预设主题中选择，如科技蓝、暖橙、墨绿）
3. 用户可设置默认着陆页（登录后先看项目/任务/经费/Dashboard）
4. 用户可配置侧边栏菜单的折叠/展开状态和顺序
5. 用户可设置表格默认每页显示条数、默认筛选条件
6. 配置跟随账户：账户 A 在电脑 1 上修改配置后，在电脑 2 上登录同一账户也保持 A 的配置
7. 账户隔离：账户 A 和账户 B 在同一台电脑上登录时，各有各的配置，互不影响

**技术方向**：

**后端**：

- 新增 `UserPreference` 模型，存储用户个性化配置（JSON 格式）：
  ```python
  class UserPreference(models.Model):
      user = OneToOneField(User, on_delete=CASCADE)
      dashboard_layout = JSONField(default=dict)  # 卡片布局
      theme = CharField(max_length=32, default='tech_blue')
      landing_page = CharField(max_length=64, default='dashboard')
      sidebar_collapsed = BooleanField(default=False)
      sidebar_order = JSONField(default=list)
      page_size = IntegerField(default=20)
      default_filters = JSONField(default=dict)
      updated_at = DateTimeField(auto_now=True)
  ```
- 新增 API：`GET/PUT /api/v1/users/me/preferences/`
- 登录响应中附带 `preferences` 字段，前端初始化时直接应用

**前端**：

- 新增 `useUserPreferences` composable，全局管理用户配置
- Dashboard 增加"编辑布局"模式，支持拖拽排序卡片
- 主题切换使用 CSS 变量动态注入，不重新加载页面
- 侧边栏顺序通过 `vuedraggable` 拖拽排序
- 配置变更时自动调用 `PUT /api/v1/users/me/preferences/` 保存
- 登录时从后端拉取配置并应用到全局 store
- 退出登录时清除本地缓存，避免账户间配置串扰

**关键约束**：

- 配置存储在后端，不依赖 localStorage（确保跨设备同步）
- localStorage 仅用作临时缓存，登录时以后端配置为准覆盖
- 主题色变更不能影响 Element Plus 组件的功能性样式
- 配置项必须有合理默认值，新用户开箱即用

### 需求 D：赛级颜色区分（v1.1.0-mobile-smart 或独立小版本）

**目标**：不同比赛等级（校赛/市赛/省赛/国赛）在项目列表、项目详情、比赛列表等位置使用不同颜色标识，提升视觉辨识度。

**背景**：当前项目列表的"当前阶段"列中，市赛、省赛、国赛均显示为灰色标签，缺乏区分度（见截图）。

**具体要求**：

1. 项目列表"当前阶段"列的赛级标签按等级着色
2. 比赛列表中的比赛级别标签同样着色
3. 项目详情页的阶段信息也使用对应颜色
4. 颜色方案（从低到高，由冷到暖/由淡到浓）：

| 赛级 | 颜色 | Element Plus type | 色值参考 |
|---|---|---|---|
| 校赛/院赛 | 蓝色 | `primary` | `#409EFF` |
| 市赛 | 青色/绿色 | `success` | `#67C23A` |
| 省赛 | 橙色 | `warning` | `#E6A23C` |
| 国赛 | 红色 | `danger` | `#F56C6C` |
| 国际赛 | 紫色 | 自定义 | `#9B59B6` |

5. 非赛级阶段（如立项、材料准备、结项）保持原有灰色或蓝色

**技术方向**：

- 前端：新建 `stageColorMap` 常量映射，根据阶段关键词匹配赛级
- 使用 Element Plus `<el-tag>` 的 `type` 属性实现颜色，无需自定义 CSS
- 匹配逻辑：阶段文本包含"校赛"→primary，"市赛"→success，"省赛"→warning，"国赛"→danger
- 在 `ProjectListView.vue`、`ProjectDetailView.vue`、`CompetitionListView.vue` 中统一应用

### 需求 E：项目详情任务 Tab 空状态修复（截图验收发现）

**目标**：项目详情页"任务"Tab 点击后内容区空白，看不到任务列表。

**背景**：截图显示项目详情页切换到"任务"Tab 后，主内容区完全空白，没有任何任务数据或空状态提示。

**具体要求**：

1. 任务 Tab 切换后应正确加载并展示该项目的任务列表
2. 如果项目确实没有任务，应显示空状态提示（如"暂无任务，点击创建"）
3. 检查前端路由参数传递是否正确（`project_id` 是否传到任务列表组件）
4. 检查后端 API `/api/v1/tasks/?project=xxx` 是否正常返回数据

**技术方向**：

- 前端：检查 `ProjectDetailView.vue` 中任务 Tab 的数据加载逻辑
- 确认 `project_id` prop 正确传递给子组件
- 添加空状态组件 `<EmptyState>` 作为 fallback

### 需求 F：比赛列表中英统一 + 距离 DDL 倒计时

**目标**：比赛列表的"级别"列中英文混用（province、city、national），统一为中文；增加距离报名截止日期的倒计时显示。

**背景**：截图显示比赛列表中级别列显示英文 `province`、`city`、`national`，应显示为"省赛"、"市赛"、"国赛"。

**具体要求**：

1. 比赛级别统一显示中文：校赛、市赛、省赛、国赛、国际赛
2. 比赛列表新增"距报名截止"列或在"比赛时间"列旁显示倒计时
3. 倒计时格式：距截止还有 X 天 / 已截止 / 进行中 / 已结束
4. 倒计时颜色：>7天灰色，3-7天橙色，<3天红色，已截止灰色

**技术方向**：

- 前端：新建 `competitionLevelMap` 常量，将后端返回的英文级别映射为中文
- 后端 `Competition` 模型确认有 `registration_deadline` 字段
- 前端使用 dayjs 计算倒计时，定时刷新（每分钟）

### 需求 G：成员头像支持证件照

**目标**：成员头像支持上传证件照，后续做 PPT 汇报时可直接导出使用，免去每次手动准备。

**背景**：截图显示成员详情页头像为灰色圆形 + 姓氏文字，无法用于 PPT 等正式场合。

**具体要求**：

1. 成员详情页和成员列表页的头像支持上传证件照图片
2. 头像上传后自动裁剪为正方形（建议 1:1，最小 200×200px）
3. 未上传头像时保持当前姓氏文字占位
4. 成员列表页头像缩小显示（32×32px），成员详情页大图显示（120×120px）
5. 支持后续"一键导出成员证件照"功能（用于 PPT 制作）

**技术方向**：

- 后端：`Member` 模型新增 `avatar` 字段（`ImageField`，上传到 `media/avatars/`）
- 前端：头像上传组件使用 `el-upload` + 图片裁剪库（如 `vue-cropper`）
- 人员列表和详情页根据 `avatar` 字段判断显示图片还是姓氏占位

### 需求 H：经费类别标签深底浅字

**目标**：经费支出明细中的类别标签改为深色背景 + 浅色文字，提升视觉对比度和辨识度。

**背景**：截图显示经费类别标签（推广费、软件费等）使用浅色背景 + 深色文字，对比度不够，辨识度低。

**具体要求**：

1. 所有经费类别标签改为深色背景 + 白色文字
2. 每个类别保持各自的主题色，但使用深色变体作为背景：
   - 报名费 → 深蓝底白字
   - 材料费 → 深绿底白字
   - 打印费 → 深蓝底白字
   - 差旅费 → 深橙底白字
   - 设备费 → 深红底白字
   - 软件费 → 深青底白字
   - 比赛报名费 → 深紫底白字
   - 推广费 → 深红底白字
   - 劳务费 → 深粉底白字
   - 其他 → 深灰底白字

**技术方向**：

- 前端：修改 `expenseCategoryColors` 常量，为每个类别提供 `bgColor` 和 `textColor`
- 使用 Element Plus `<el-tag>` 的 `effect="dark"` 属性实现深色背景

### 需求 I：经费支出明细列调整 + 报销状态

**目标**：经费支出明细表格新增"是否已报销"列，并调整列顺序为：说明-金额-类型-经手人-日期-票据-是否已报销。

**背景**：截图显示当前列顺序为：日期-类别-说明-金额-经手人-状态-票据，用户希望调整顺序并增加报销状态列。

**具体要求**：

1. 列顺序调整为：说明 → 金额 → 类型 → 经手人 → 日期 → 票据 → 是否已报销
2. "是否已报销"列显示为开关或标签（已报销/未报销/覆盖中）
3. 报销状态支持点击切换
4. 保留原有的"状态"列（审批状态），但与新"是否已报销"列区分

**技术方向**：

- 后端：`Expense` 模型新增 `is_reimbursed` 字段（BooleanField，默认 False）
- 后端：新增 `reimbursement_status` 枚举（未报销/已报销/覆盖中）
- 前端：调整表格列顺序，新增报销状态列

### 需求 J：贡献记录"蓝概括绿详细"样式规范

**目标**：贡献记录列表中，贡献类型列用蓝色标签概括，贡献内容列用绿色高亮详细描述，形成统一的"蓝概括绿详细"视觉规范。

**背景**：截图中用户用蓝色框标注了"贡献类型"列，用绿色框标注了"贡献内容"列，提出是否统一为蓝色概括、绿色详细。

**具体要求**：

1. 贡献类型标签统一使用蓝色系（浅蓝底深蓝字）
2. 贡献内容高亮使用绿色系（浅绿底深绿字或绿色左边框）
3. 此规范扩展到其他列表页：类型/状态等概括信息用蓝色，详细内容用绿色
4. 形成全局统一的"概括蓝、详细绿"视觉语言

**技术方向**：

- 前端：定义全局 CSS class `.tag-summary`（蓝色系）和 `.tag-detail`（绿色系）
- 在贡献记录、任务、经费等列表页统一应用
- 更新 `theme-factory` 主题变量中的 `--color-summary` 和 `--color-detail`

### 需求 K：个人中心跳转修复

**目标**：点击"个人中心"应跳转到当前登录用户的个人详情页，而非全部成员列表。

**背景**：截图显示点击个人中心后跳转到了人员管理列表页（显示所有成员），而非当前用户自己的个人信息页。

**具体要求**：

1. 点击用户头像下拉菜单中的"个人中心"应跳转到 `/members/{当前用户ID}`
2. 个人中心页面显示当前用户自己的详情（头像、信息、参与项目、贡献记录等）
3. 个人中心页面可编辑自己的基本信息（联系方式等非敏感字段）
4. 敏感字段（身份证号等）仍需审批才能查看/修改

**技术方向**：

- 前端：修改用户下拉菜单"个人中心"的路由跳转，使用当前用户 ID
- 确认 `MemberDetailView.vue` 支持以当前用户 ID 加载
- 后端：确认 `/api/v1/members/{id}/` 接口对本人访问返回完整信息

---

## 6. 每次提交前检查清单

- [ ] 是否更新本文档；
- [ ] 是否更新 CHANGELOG；
- [ ] 是否需要更新 VERSION；
- [ ] 是否需要更新 seed 数据；
- [ ] 是否需要更新 screenshots；
- [ ] 是否跑过前端类型检查；
- [ ] 是否跑过前端构建；
- [ ] 是否跑过后端 check；
- [ ] 是否做过权限回归；
- [ ] 是否确认没有 `NaN / undefined / 冒号表头 / 标题竖排 / PC 侧栏出现在移动端`。

### 修改记录：2026-07-02 17:45 左右

操作者：Codex / 人工指令
修改目标：尝试初始化功能落地总表与本地 Docker 验证配置
当前实际结果：
- 正式功能落地总表此前未创建，已补建；
- 发现模板文件：功能落地总表与修改记录模板.md；
- 发现 Docker 相关文件：docker-compose.yml、.env.docker、nginx/default.conf；
- 未发现 backend/Dockerfile、frontend/Dockerfile；
- 发现 deploy/dockerfiles/backend.Dockerfile；
- backend/requirements/base.txt 已修改，末尾已无整行 pip install 命令；
- V2 seed 文件仍存在；
- seed assets 仍存在；
- PostgreSQL 备份 backend/team_management.before_v2_seed.dump 仍存在；
- media 备份 backend/media.before_v2_seed 仍存在；
- Docker daemon 当前不可连接，无法确认容器、镜像、volume 状态。

验证结果：
- 未执行新的 Docker build；
- 未执行新的 Docker up；
- 未执行新的 docker compose down -v；
- 未执行新的 seed --clean --force；
- 未确认 Docker 环境可用。

剩余问题：
- Docker Desktop / daemon 不可连接；
- docker compose ps 报 project name must not be empty；
- C:\Users\DaiYuan\.docker\config.json 权限拒绝；
- Docker 容器和 volume 状态无法确认；
- 还没有完成本地 Docker 验证；
- 还没有完成全量截图验收。

下一步：
- 等 Docker Desktop 正常启动；
- 使用明确项目名执行 docker compose -p team_management_local config；
- config 通过后再 build/up；
- build/up 之前不执行 down -v。

### 修改记录：2026-07-02 20:00 左右

操作者：Codex / 人工指令
修改目标：继续本地 Docker 验证，并修复部署配置层阻断问题

当前实际结果：
- 已创建项目本地 Docker 配置目录 `.docker-config`，并将 `config.json` 修正为无 BOM 的 `{}`；
- `docker compose -p team_management_local config` 可通过；
- 修复 `backend/requirements/base.txt`，恢复 `django==5.0.6` 为有效依赖，确认末尾无 `pip install ...` 命令；
- 修复 `backend/requirements/dev.txt`，恢复 `-r base.txt`；
- 修复 `deploy/dockerfiles/backend.Dockerfile`，恢复 `ENV` 与 `RUN mkdir -p /app/media /app/staticfiles` 为有效 Docker 指令；
- 修复 `backend/config/settings/base.py`、`dev.py`、`prod.py` 中注释吞掉代码的问题，恢复 app 注册、中间件、REST Framework、SimpleJWT、Celery、静态文件与媒体路径配置；
- 未修改业务逻辑、API 字段、权限规则、登录 token 结构、审批流程或经费公开规则；
- 未删除 V2 seed、真实数据、media 备份或 Docker volume。

验证结果：
- Python settings 文件语法检查通过；
- `docker compose -p team_management_local config` 通过；
- `docker compose -p team_management_local build` 未通过，原因是当前 Codex 沙箱访问 Docker daemon 报 `permission denied while trying to connect to the docker API at npipe:////./pipe/docker_engine`；
- 已按授权流程请求提升权限访问 Docker daemon，但自动审批系统返回 403，无法继续 build/up；
- 未执行 Docker up；
- 未执行 migrate；
- 未执行 collectstatic；
- 未执行 manage.py check；
- 未执行 seed --clean --force；
- 未执行 API 验证；
- 未执行浏览器截图验收。

剩余问题：
- Codex 当前会话无法访问 Docker daemon；
- Docker build/up/migrate/seed/API/截图验收仍未完成；
- 需要用户在本机授权 Codex 访问 Docker daemon，或由用户在普通 PowerShell 手动执行 Docker 命令后把结果交回。

下一步：
- 先确认 `docker ps` 在用户普通 PowerShell 中可正常执行；
- 允许 Codex 以提升权限执行 `docker compose -p team_management_local build`；
- build 通过后再执行 `up -d`、`ps`、日志检查、migrate、collectstatic、check、V2 seed、数据核验、API 验证和截图验收；
- 继续禁止 `docker compose down -v`，除非用户再次明确确认。
