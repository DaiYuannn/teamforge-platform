# ============================================================
# 前端环境变量模板
# 使用方法: 复制为 frontend.env 并按需修改（start-dev 脚本会自动复制）
# ============================================================

# API 基础地址（前端请求前缀，由 Nginx 反向代理到后端）
VITE_API_BASE_URL=/api/v1
