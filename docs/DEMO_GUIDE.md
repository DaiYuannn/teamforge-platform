# 团队管理软件演示指南

> 适用版本：v2.1.0
> 发布日期：2026-07-29
> 演示数据入口：`python manage.py seed_demo_data --clean --force`

本指南面向演示、验收和上线前试运行。演示环境只使用模拟账号、模拟票据和模拟敏感资料，禁止在完成权限与恢复演练前导入真实成员证件或财务资料。

小团队分支的服务器拉取、Docker 部署、最终数据生成、验证和备份步骤见
[`DEPLOY_SMALL_TEAM.md`](./DEPLOY_SMALL_TEAM.md)。

## 1. 启动环境

### 1.1 依赖

- Python 3.10+
- Node.js 18+
- PostgreSQL 14+
- Redis 7+
- Tesseract OCR（容器镜像已安装中文语言包）

PDF 由 ReportLab 生成，不依赖 WeasyPrint 或 GTK。

### 1.2 后端

```bash
cd backend
pip install -r requirements/dev.txt
python manage.py migrate
python manage.py seed_demo_data --clean --force
python manage.py runserver
```

升级既有环境时确认已应用最新迁移；其中
`intellectual_property.0004_alter_intellectualpropertyapplication_ip_type`
增加“科技查新”成果类型。

后端默认地址：`http://127.0.0.1:8000`。

### 1.3 前端

```bash
cd frontend
npm install
npm run dev
```

前端默认地址：`http://127.0.0.1:3000`。

### 1.4 定时任务

本地演示定时提醒和定时报表时，同时启动 Worker 与 Beat：

```bash
cd backend
celery -A config worker -l info
celery -A config beat -l info
```

Docker 生产编排默认启动这两个服务，无需额外 profile。

## 2. 演示数据规模

完整种子生成一套确定性的团队数据：

| 数据 | 规模 |
|------|------|
| 登录账号 | 47 个（另有总团队人员 46 人，技术管理员不计入团队） |
| 团队项目 | 7 个：2 个历史退役项目 + 5 个当前项目 |
| 项目任务 | 35 个 |
| 比赛 | 33 个比赛届次、109 条项目参赛记录 |
| 贡献记录 | 15 条，覆盖待审核、通过和驳回 |
| 项目与比赛附件 | 247 个，含逐届独立计划书、PPT 和历史版本 |
| 成果档案 | 40 个专利、25 个软著、7 个科技查新 |
| 模拟票据 | 每条演示支出 1～2 张 PNG |
| 定时报表 | 3 个计划及 XLSX、DOCX、PDF 成功执行文件 |

`seed_demo_data` 是完整演示环境的唯一入口。
`seed_competition_demo` 目前是兼容别名，会转交给同一套完整种子，不会叠加另一套数据。

## 3. 演示账号

| 账号 | 密码 | 用途 |
|------|------|------|
| `admin@demo.com` | `admin123456` | 系统管理、备份、全局数据 |
| `teacher1@demo.com` | `teacher123456` | 唯一操作老师、全局业务兜底、紫色账户主题 |
| `teacher2@demo.com` 至 `teacher4@demo.com` | `teacher123456` | 偶尔登录查看数据和团队情况 |
| `leader1@demo.com` 至 `leader5@demo.com` | `leader123456` | 五支当前项目队伍的负责人流程 |
| `contributor1@demo.com` 至 `contributor2@demo.com` | `member123456` | 与负责人平行的学生主贡献者 |
| `member1@demo.com` 至 `member35@demo.com` | `member123456` | 普通成员流程 |

系统不提供 2FA。GitHub、Google 等第三方登录仅保留配置预留，未配置时不能作为登录方式。

## 4. 推荐验收顺序

### 4.1 账户偏好与隔离

1. 使用 `admin@demo.com` 登录，确认主色和明暗模式为管理员保存的设置。
2. 进入 `/user/preference`，依次预览浅色、深色和跟随系统；确认页面、弹窗、表格及图表同步切换且文字可读。
3. 选择定时模式并设置跨午夜时段（例如 `19:00` 至 `07:00`），保存后刷新页面，确认设置仍由服务器恢复。
4. 修改主色、默认着陆页、每页条数、默认数据范围、侧栏状态、常用入口、菜单顺序和通知偏好。
5. 退出并登录 `teacher1@demo.com`，确认主色、明暗模式和布局自动切换为老师账户自己的设置；退出后恢复跟随系统。
6. 检查通知铃铛中没有上一账户的数据，默认着陆页按新账户设置跳转。

偏好保存在服务器，不依赖当前浏览器的本地缓存。

### 4.2 项目、比赛和时间线

1. 进入 `/projects`，查看 7 个项目及成员、阶段和负责人更新；其中 2 个历史项目已在国金/国银后结项，5 个项目仍在进行。
2. 打开项目详情，推进阶段并填写进度；确认时间线产生“阶段变更”。
3. 新建、完成任务并上传文件；确认出现“任务创建”“任务完成”“文件上传”。
4. 进入 `/competitions`，切换 2021—2026 届次；核对大创、大挑、小挑、国创在 2024 年起均有五支项目队，数字中国和厦门银行杯每届只有一至三支项目队。
5. 在时间线多选“任务”“比赛”“贡献”等分类，确认筛选结果没有英文事件代码。
6. 进入 `/dashboard/calendar`，确认任务截止、项目启动、项目结束、经费支出均显示中文标签。

### 4.3 成员、比赛任务与有效工作量

1. 进入 `/members` 和 `/team`，查看 46 人总团队，并按身份、学校、专业和状态组合筛选。
2. 普通成员进入 `/members/schedule`，选择比赛届次和参赛项目，查看私下确认后登记的任务、DDL、协作人、子任务与验收状态。
3. 负责人进入 `/members/team-schedule`，按同一比赛参赛条目发布透明的有效工作量占比；总和必须为 100%，时间跨度只作为参考。
4. 普通成员对已发布占比提出异议，负责人核对后答复；新结论通过发布新版本保留历史。
5. 演示成员退出、负责人交接和交接说明，确认历史成员关系仍可追溯。

### 4.4 贡献、排序与异议

1. 项目成员进入 `/contributions` 提交本项目真实贡献及证明文件。
2. 项目负责人进入 `/contributions/pending` 审核贡献。
3. 负责人依据审核通过的贡献生成排序草案。
4. 项目负责人确认并向项目成员公开排序；操作老师仅作为全局兜底。
5. 成员提交异议，一人初审、另一位负责人最终复核；提出人、初审人不能复核自己的处理，检查所有意见和调整快照均被保留。

排序以当前项目贡献为核心，不使用跨项目总分直接替代本项目判断。

### 4.5 经费与票据 OCR

1. 进入 `/finance`，在“项目视角”查看同一项目参加多场比赛的收入、奖金和支出，再切到“比赛视角”查看同一比赛下多个参赛队。
2. 点击一条参赛记录，核对奖金预计、确认应收、实际到账、报销申请、发票、付款和完整时间线。
3. 点击“票据 OCR”，上传清晰的 PNG/JPEG/PDF 模拟票据，检查金额、日期、票据号和商户并人工修正低置信度字段。
4. 提交报销后确认额度进入预留；申请人、垫付人和收款人不能自审。
5. 经办人可分次付款；只有填写流水号并上传转账凭证后才能计为团队实际支出。
6. 模拟付款异常、冲正和内部转移，确认内部转移不会重复计为收入或支出。

OCR 结果必须人工复核，识别结果不能直接作为财务凭证。

### 4.6 文件和知识产权

1. 进入 `/files`，创建项目文件夹，给文件分配标签并在同一项目内移动文件。
2. 分别预览或下载演示 PDF、Word、Excel 和 PPT 文件，确认 Office 预览只提取有界只读内容。
3. 上传新版本并恢复历史版本，确认文件版本和审计记录同步更新。
4. 创建并撤销非敏感文件分享；确认敏感文件不能创建公开分享。
5. 删除文件后进入回收站验证恢复，并按角色检查永久删除权限。
6. 进入 `/intellectual-property`，核对 40 个专利、25 个软著和 7 个科技查新，并演示材料撰写、负责人审核、老师确认、退回修改、受理、授权和归档。
7. 检查成果节点进入项目时间线，最终证书是独立 PDF 文件。

### 4.7 敏感资料

1. 普通成员进入 `/sensitive`，默认只能看到脱敏内容。
2. 提交查看申请并说明用途。
3. 使用唯一操作老师 `teacher1@demo.com` 在 `/sensitive/pending` 审批并设置有效期。
4. 切回申请账号，在有效期内查看或下载；过期后再次访问应被拒绝。
5. 在 `/audit/logs` 核对申请、审批、查看和下载记录。

### 4.8 实时通知

1. 同时打开负责人和成员两个会话。
2. 在一个会话创建任务、贡献审核或报表执行结果。
3. 另一个会话应在不刷新页面的情况下收到站内通知。
4. 点击通知直接进入关联项目、任务、比赛、经费或报表页面。
5. 一个终端标记已读后，另一个终端应同步更新；断开 Redis 后恢复连接，数据库补偿不能漏掉通知。

### 4.9 定时报表

1. 进入 `/reports`，查看每日、每周和每月计划。
2. 下载已有 XLSX、DOCX、PDF 执行文件。
3. 创建计划，设置数据源、频率、时间、格式和接收人。
4. 创建者、老师或管理员可编辑、启停、立即运行和删除；仅接收人只能查看和下载。
5. 立即运行后检查执行状态、站内通知和文件下载。

### 4.10 演示备份恢复

1. 管理员进入 `/admin/backups` 创建备份包。
2. 下载 ZIP，核对 `manifest.json`、业务快照、附件和 SHA-256 清单。
3. 修改一条演示业务数据，再选择刚才的包执行恢复。
4. 系统应先生成回滚包，然后恢复所选包当时的数据和附件。
5. 恢复后本地登录状态被清除，需要重新登录。
6. 再选择回滚包恢复，确认可以回到恢复操作前的状态。

该入口只管理 `seed_demo_data` 所有的演示数据，不替代生产数据库和媒体灾难恢复。生产恢复流程见 `docs/BACKUP_GUIDE.md`。

### 4.11 v2.1 协作、分析与平台工作台

1. 从任务列表进入 `/tasks/:id/collaboration`，检查子任务、依赖、评论、回复和成员提及。
2. 从项目详情进入 `/projects/:id/operations`，检查里程碑、风险、讨论、知识、复盘、模板和四类智能分析。
3. 进入 `/analytics-studio`，检查自定义看板、报表生成、智能周报和 Markdown 导出。
4. 进入 `/admin/platform-capabilities`，分别使用管理员和普通成员确认可写操作会按角色降级。
5. 使用系统管理员进入 `/admin/engineering`，检查真实性能采样、OpenAPI 索引和质量门禁；普通成员不得访问系统元数据。
6. 在桌面与移动视口重复以上路径，检查无空白页、重叠、横向溢出或桌面侧栏误显示。

## 5. 上线前检查

```bash
cd frontend
npm audit --audit-level=high
npm run lint
npm run type-check
npm test
npm run build
npm run test:e2e

cd ../backend
python -m flake8 .
python manage.py check
python manage.py makemigrations --check --dry-run
python manage.py spectacular --file openapi.yml --validate
pytest
```

还需在桌面和移动端完成登录、账户切换、浅色/深色/系统/定时主题、时间线筛选、OCR、通知、报表、备份恢复及 4.11 所列 v2.1 工作台的浏览器验收。上述命令和页面的最终结果应回填至发布记录；未执行的 CI、生产服务器、Celery Worker/Beat 或真实第三方集成不得标记为通过。测试失败、迁移漂移、恢复演练失败或敏感资料权限不符合预期时，不得导入真实数据。

## 6. 常见问题

### 前端端口是什么？

Vite 默认使用 `3000`，后端默认使用 `8000`。

### PDF 是否需要 GTK？

不需要。当前 PDF 使用 ReportLab 和 CID 中文字体。

### 为什么没有收到定时提醒？

检查 Celery Worker、Beat 和 Redis。实时站内通知还需检查 SSE 请求与 Nginx 的 `X-Accel-Buffering` 配置。

### 为什么 OCR 识别不准？

优先使用清晰、方向正确、无遮挡的原图。系统会返回置信度并要求人工确认；过大、像素异常或处理超时的文件会被拒绝。

### 可以直接用演示备份恢复生产数据吗？

不可以。演示备份只处理带有明确演示所有权标记的数据。生产数据库、媒体文件和密钥必须按生产备份指南单独恢复。
